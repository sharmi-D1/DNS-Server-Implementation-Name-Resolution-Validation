# DNS-Server-Implementation-Name-Resolution-Validation
Implemented and configured a DNS Server using Windows Server 2022 to understand domain name resolution, DNS zone management, and IP mapping in a virtual lab environment. Validated DNS functionality using a Kali Linux client by performing DNS queries, analyzing responses, and troubleshooting resolution-related issues such as NXDOMAIN errors.
